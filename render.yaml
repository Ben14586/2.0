services:
  - type: web
    name: game-services
    runtime: docker
    plan: free
    healthCheckPath: /api/games
    disk:
      name: game-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 3000
      - key: HOST
        value: 0.0.0.0
